#include <stdio.h>
int main(){
    int n;
    scanf("Enter the number")
    int s = sizeof arr;
    for(int i=0; i<n; i++){
        scanf("%d", &arr[i]);
    }
    for(int i=0; i<s; i++){
        sum += arr[i];
    }
    printf("%d", sum);
    return 0;
}