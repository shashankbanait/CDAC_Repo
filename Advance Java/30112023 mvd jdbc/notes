MVC (Model - View - Controller)
Model : it is the component of MVC which contain business logic to process the request render by the controller and interact with database
        with the help of DAO(data access object) and generate response for controller

View : view is the component in which we put presentation logic and responsible to produce final 
it is the most important component of mvc which is responsible to take decision on the basis of 
request from the client and response generated by it's corresponding model, that which view should be render to the client