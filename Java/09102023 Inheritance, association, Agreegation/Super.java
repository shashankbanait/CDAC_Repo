public class Super {
    // it is a keyword which is responsible to ovvercocme variable hiding, which is generratoed due to 
    // same instance variable of derived class and base class also.
    // it is useful to call base class constructor
    // it is also usefull to call base class overriden method
    
}
